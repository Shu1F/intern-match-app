import React, { useState } from "react";

type messageType = {
  content: string;
};

const MessagePage = () => {
  const [content, setContent] = useState(<mess>);
  return;
};

export default MessagePage;
